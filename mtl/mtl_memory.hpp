#pragma once

#include <typeinfo>

namespace mtl
{
	//template <typename T>
	//T* addressof(T& arg)
	//{
    //    return std::reinterpret_cast<T*>(&const_cast<char&>(std::reinterpret_cast<const volatile char&>(arg)));
	//}
}
